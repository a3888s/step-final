# Використання базового образу Python версії 3.10 (легкий варіант)
FROM python:3.10-slim

# Встановлення робочого каталогу всередині контейнера
WORKDIR /app

# Копіювання файлу app.py у робочий каталог контейнера
COPY app.py .

# Відкриття порту 8080 для роботи додатка
EXPOSE 8080

# Команда для запуску додатка при старті контейнера
CMD ["python", "app.py"]
